package app.services;

public class FavoriteService {
    public static boolean isFavorite(Long userId, Long adId) throws Exception {
        // ارسال درخواست به سرور
        return false; // فعلاً
    }
    public static void addFavorite(Long userId, Long adId) throws Exception {
        // ارسال درخواست به سرور
    }
    public static void removeFavorite(Long userId, Long adId) throws Exception {
        // ارسال درخواست به سرور
    }
}
